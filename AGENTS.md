# Artigen AI 开发规则

本文件适用于整个 Artigen 仓库。任何 AI 在分析、诊断、修改、测试、Review 或发布本仓库前，都必须遵守本文件。

## 1. 任务开始

开始任何 Artigen 任务时，按顺序执行：

1. 读取本文件。
2. 读取 [`PROJECT_HANDOFF.zh-CN.md`](./PROJECT_HANDOFF.zh-CN.md)，了解已经固化到 GitHub 的正式项目状态。
3. 读取根目录 `HANDOFF.local.md`；如果不存在，立即按第 3 节的结构创建。
4. 执行 `git status --short --branch`，识别用户已有修改、未跟踪文件和当前分支。
5. 把必须保留的用户工作树内容写进 `HANDOFF.local.md`，不得覆盖、删除、格式化、暂存或提交无关改动。

`HANDOFF.local.md` 是开发过程真相，`PROJECT_HANDOFF.zh-CN.md` 是 GitHub 正式项目真相。两者不能混用。

## 2. 双层 Handoff 原则

### 2.1 本地阶段性 Handoff

`HANDOFF.local.md`：

- 每个以 Artigen 仓库为工作对象的 AI 任务都必须维护，包括只读分析、诊断、Review、开发中断和阻塞任务。
- 是滚动的当前快照，可以记录尚未完成的阶段、具体调试线索、临时尝试和下一条命令。
- 在任务结束前必须更新；长任务还要在上下文压缩、阶段切换或高风险操作前更新检查点。
- 已被 `.gitignore` 忽略，永远不能进入 Git、PR 或 GitHub。
- 不能作为生产状态、发布完成或正式架构的证据。

### 2.2 GitHub 正式 Handoff

[`PROJECT_HANDOFF.zh-CN.md`](./PROJECT_HANDOFF.zh-CN.md)：

- 只记录已经确定并产生持久影响的代码、配置、迁移、部署、运行方式和用户批准的正式决定。
- 有持久影响的改动必须在同一 PR 中同步更新，不能长期事后补写。
- 不记录开发过程中比较过但未采用的方案、临时实验、已撤销修改、猜测或聊天流水账。
- Bug 修复只写最终问题、最终修复、影响范围和验证结果。
- 尚未部署的内容必须标记为“已提交”“已合并但未部署”或其他准确阶段，不能写成“已上线”。
- 生产事实只有经过 `/api/meta`、`/readyz`、状态接口或平台 deployment 验证后才能固化。
- 后续修补优先修正同一主题的现有结论，避免产生相互矛盾的正式记录。

## 3. `HANDOFF.local.md` 固定结构

本地文件不存在时创建以下章节；存在时更新为当前快照，不无限追加历史聊天：

```markdown
# Artigen 本地开发 Handoff

更新时间：

## 当前任务
## 目标与成功标准
## Git 与工作树
## 必须保留的用户改动
## 已完成
## 当前精确状态
## 修改与待修改文件
## 测试、证据与未验证项
## 临时尝试及处理结果
## 阻塞项与待确认决策
## 下一位 AI 的具体步骤
## 远程 Handoff 判定
## 敏感信息边界
```

最低记录要求：

- 当前 branch、base 和 HEAD；
- 当前任务目标和可验证的成功标准；
- 用户已有且不属于本任务的工作树内容；
- 本轮已经完成和尚未完成的具体事项；
- 修改过和计划修改的文件；
- 已运行测试的完整命令、结果和没有运行的测试；
- 临时尝试为什么失败、是否已经撤销；
- 下一位 AI 可以直接执行的步骤；
- 本任务是否需要更新正式 Handoff，以及判断原因。

## 4. 什么时候更新哪份 Handoff

| 场景 | 本地 Handoff | 正式 Handoff |
| --- | --- | --- |
| 只读代码分析 | 必须更新 | 不更新 |
| 诊断但未修复 | 必须更新 | 不更新 |
| 开发进行中、尚未形成最终实现 | 必须更新 | 不更新 |
| 完成代码、配置或文档改动 | 必须更新 | 必须更新 |
| 用户确认正式架构决定 | 必须更新 | 必须更新 |
| Bug 修复 | 必须更新 | 写最终修复摘要 |
| PR Review 后的小修 | 必须更新 | 必要时修正原结论 |
| DEV 部署 | 必须更新 | 有验证证据后更新 |
| Production 部署 | 必须更新 | 生产核验完成后更新 |
| 方案被放弃并完全撤销 | 必须更新 | 不更新 |
| 与 Artigen 无关的一般问答 | 不要求 | 不更新 |

如果 PR 没有更新正式 Handoff，必须在 PR 模板中写明准确的不适用原因。纯粹写“无需更新”不算原因。

## 5. 任务结束门禁

AI 在发送最终回复前必须完成：

1. 重新检查 `git status --short --branch`。
2. 更新 `HANDOFF.local.md` 为实际结束状态，而不是计划状态。
3. 确认本地 Handoff 仍被 Git 忽略且没有进入暂存区。
4. 如有持久改动，更新 `PROJECT_HANDOFF.zh-CN.md`；如没有，记录不更新的原因。
5. 记录已运行测试、失败、跳过和未验证项，不能把未执行的检查写成通过。
6. 确认没有混入用户无关改动。
7. 最终回复明确说明交付物、验证结果和仍存在的限制。

如果文件系统不可写，无法更新本地 Handoff，必须在最终回复中明确说明，不能静默跳过。

## 6. GitHub 和 PR 规则

- 日常功能和修复从功能分支通过 PR 进入 `dev`，验证后再由 `dev` 进入 `main`。
- 不直接 push `dev` 或 `main`，不绕过 Release gate。
- 有持久影响的 PR 必须包含正式 Handoff 更新；缺失或与代码/部署证据矛盾时应阻止合并。
- `HANDOFF.local.md` 只能在本机存在，不得使用 `git add -f` 强行提交。
- 正式文档只写最终结论。开发阶段的“先这样、后那样”只留在本地 Handoff。
- 修 Bug 或补丁可以更正正式结论，但必须说明最终行为和验证证据，不能保留已知错误事实。
- 不以更新 Handoff 为理由暂存、提交或格式化无关用户文件。

详细分支、测试和发布规则见 [`CONTRIBUTING.md`](./CONTRIBUTING.md) 与 [`PROJECT_OPERATIONS_GUIDE.zh-CN.md`](./PROJECT_OPERATIONS_GUIDE.zh-CN.md)。

## 7. 敏感信息和生产边界

两份 Handoff 都禁止出现：

- 密码、邮箱或短信验证码；
- API Key、Token、Cookie、恢复码；
- 数据库连接串或包含凭据的 URL；
- Keychain 中读取出的真实值；
- 平台 Secret、Environment Export 或私钥。

可以记录账号标识、资源 ID、环境变量名称、Keychain service/account 标签和登录入口，但不能记录秘密值。

生产事实的优先级：

1. 当前线上只读接口和平台 deployment；
2. GitHub `main` 与实际部署 SHA；
3. 当前代码和数据库迁移；
4. 正式 Handoff 和专题运维文档；
5. 本地 Handoff；
6. 早期聊天或历史计划。
