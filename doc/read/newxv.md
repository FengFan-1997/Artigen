现在还是与agent融合不够，就是我们的vrm模型，首先每个模型要跟/Users/fengfan/Documents/newPro/doc/modeDoc性格文档做一个对应，比如当前是八重神子，那么对应的就是八重神子的性格特点什么的，包括语音，动作，表情等等，都要对应上，然后我们的agent要根据用户的输入，来判断用户的性格，然后根据性格来生成对应的动作，语音，表情等等。
这个是怎么搞呢，就是首先用户不操作的时候，我们也是需要偶尔调用一下api，然后让ai根据性格文档告诉我们 神子在闲暇的时候会干什么，然后让ai返回一些json参数，然后我们根据这些参数，来控制神子的动作，语音，表情等等。这是一层逻辑，其次就是agent的核心了，就是任务处理  用户通过跟我们的agent对话，然后我们收集用户的输入，然后一同将性格文档，我们模型的使用手册（得告诉ai返回什么才能让模型动起来，所以肯定得有使用文档）给到ai，然后让ai返回这些json然后我们呈现，并且这个过程是持续的，因为我们比如每到一个节点或者是每次完成一个动作队列，我们就需要再次请求ai 来获取下一次动作，因为一个任务肯定是不止一个动作不止一个语句不止一个表情。而且我的agent也需要根据页面上面的变化而做出反应。举个例子，我们去告诉agent说让他演示一下营养配料表的生成（我的一个项目）然后agent首先先反应一会，然后说些东西，做些动作，然后肯定会跳转页面的，这个跳转页面能不能让agent继续工作应该也很复杂，其次跳转页面，agent起码得实时收集当前页面上面的各种dom bom然后观察变化，比如生成的时候报错了，ai agent该说什么，成功了该怎么，这些非常复杂，这个是一个超级大工程，你需要分好多文件，分好多次实现这个需求，并且每次实现需求都需要生成一个文档，叫做 agent文档1， 2 ，3以此类推