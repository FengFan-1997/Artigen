
已把「线上用户注册/生图/使用记录」接进你的后台 console，数据来自后端真实存储，不再是 localStorage 假数据。

**已完成的改动**
- 后端新增/完善 admin 数据接口（统一用 `x-admin-key` 鉴权）
  - 用户列表（含积分钱包）：`GET /api/admin/users`
  - 生图历史（全站或按 userId）：`GET /api/admin/images/history`
  - 用量台账（全站或按 userId）：`GET /api/admin/usage/ledger`（本次新增）  
  代码位置：[server.js](file:///g:/AvuePro/newPro/backend/server.js#L3286-L3336)、[server.js](file:///g:/AvuePro/newPro/backend/server.js#L3997-L4043)、[server.js](file:///g:/AvuePro/newPro/backend/server.js#L4373-L4418)、[server.js](file:///g:/AvuePro/newPro/backend/server.js#L5112-L5192)
- 前端 console 增加 Admin Key 输入与持久化，并改为拉取后端真实数据
  - 登录页新增 Admin Key 输入，登录成功后保存并用于请求头 `x-admin-key`：[ConsoleLayout.vue](file:///g:/AvuePro/newPro/frontend/src/console/ConsoleLayout.vue)
  - console store 新增 `adminKey/adminUsers/adminImages/adminUsage` 以及对应拉取方法：[console.ts](file:///g:/AvuePro/newPro/frontend/src/stores/console.ts)
  - 三个页面改用后端数据源：
    - 用户管理（真实注册用户 + 积分钱包）：[UserManagement.vue](file:///g:/AvuePro/newPro/frontend/src/console/views/UserManagement.vue)
    - 内容审计（真实生图历史 + 调用记录）：[ContentAudit.vue](file:///g:/AvuePro/newPro/frontend/src/console/views/ContentAudit.vue)
    - 使用记录（真实 usage ledger，支持分页/时间范围/按 userId 过滤）：[Usage.vue](file:///g:/AvuePro/newPro/frontend/src/console/views/Usage.vue)

**你在 Zeabur 上需要做的配置**
- 后端服务环境变量设置：`ADMIN_KEY=<一个强随机字符串>`
- 如果前后端不是同域：
  - 前端设置 `VITE_API_BASE=https://你的后端域名`（console 会用它拼出 `/api/...` 请求）
- 使用方式：
  - 打开 `/console` 登录（原账号密码逻辑不变）
  - 额外填入 Admin Key（和 Zeabur 后端 `ADMIN_KEY` 一致）
  - 然后在：
    - `/console/users` 看真实注册用户与积分余额
    - `/console/audit` 看真实生图记录（预览图来自 `/files/...`）
    - `/console/usage` 看真实使用明细（tokens/credits/model/trigger 等）

**验证**
- 已在本地跑过 `pnpm -C frontend lint` 与 `pnpm -C frontend type-check`，均通过。