## PRD（UI/UX 与定价权）— 视觉叙事驱动的可盈利产品

### 0. 目标与原则

目标：把“文生图 Prompt 助手”做成一个愿意付费的产品，而不是一个“能用但不值钱”的工具。

原则：
- UI/UX 是定价权的一部分：视觉、动效、文案共同塑造“值不值”的直觉。
- 产品即叙事：每一个按钮、每一次加载、每一次失败提示，都是叙事的一部分。
- 清晰优先于酷：叙事可以酷，但必须可理解、可预测、可控。
- 成本可控：每一次“深度思考/换一批/再来一张”都等价于真实成本，需要规则。

---

### 1. 视觉基调（参考 https://www.nthme.org/ 的 UI/UX 气质）

视觉基调：Steins;Gate × Cyberpunk 2077 × Neo‑Brutalist  
关键词：电流、终端、实验室、稀缺、仪表盘、噪声纹理、玻璃与硬边并存。

色彩系统（建议）：
- 背景：近黑（避免纯黑导致的压迫与层级丢失）
- 主强调：电光绿（Acid Green）
- 次强调：电紫/电蓝（用于层级、选中、次要状态）
- 错误：高对比红（必须可读，避免“酷但看不清”）
- 成功：青绿偏蓝（与主强调区分）
- 边框/分割：带噪声的灰绿/灰蓝

字体系统（建议）：
- 标题/关键数字：几何无衬线
- Prompt/参数/日志：等宽字体（强化“终端/实验室”叙事）
- 长文说明：易读无衬线 + 足够行高

形态语言（建议）：
- 组合：硬边（Neo‑Brutalist）+ 轻玻璃（只用于浮层/面板，不要全站磨砂）
- 按钮：方形/切角、强边框、明确 hover/active
- 层级：少用阴影，多用描边、噪声、半透明分层

---

### 2. 核心体验（让“愿意付费”的体验落地）

核心工作流三段式：
1) 输入：一句话也能开始（降低门槛）
2) 决策：让用户选方向，而不是写提示词（减少失败率）
3) 产出：可复现、可迭代、可收藏分享（形成资产感）

关键交互组件（必须做精）：
- 输入框：示例/一键填充、历史输入、快捷标签（风格/画幅/用途）
- 深度模式开关：明确“更慢/更贵/更可控”
- 方向卡片：4 个选项 + 换一批（标题 + 关键视觉词 + 预计风格）
- Prompt 面板：默认隐藏复杂度，允许“高级展开”
- 参数面板：尺寸/张数/seed/步数/负面提示词，提供安全默认值
- 结果画廊：对比/收藏/下载/复制 Prompt/再来一张

加载与失败体验（决定付费留存）：
- 加载分阶段：分析 → 组合 → 渲染（让等待有意义）
- 失败可操作：重试/降级/切模型/关闭深度模式
- 降级兜底：模型不可用时，至少输出方向与 Prompt，不中断流程

---

### 3. 文案调性（叙事 + 合规双轨）

你原本的“观测者叙事”适合差异化与定价权，但商业产品需要双轨：

A. 叙事文案（主界面）
- 身份：观测员（Observer）
- 支付：注入算力能源（Energy）
- 产出：启动渲染/解锁权限/获得观测结果

B. 合规文案（支付/权益/条款）
- 必须清晰：套餐、价格、有效期、消耗规则、退款规则、自动续费（如有）
- 叙事可做副标题，但不能替代清晰说明

---

### 4. 付费策略（以“能量/积分”为统一计费单位）

目标：建立可计费、可对账、可运营的成本闭环。

建议的商业模型（从易到难）：
1) 充值包（最易上线，纠纷少）
2) 月订阅（稳定现金流，需要完整的权益/风控/退款策略）
3) 专业版（团队/商用授权/更高并发/私有风格库）

付费墙最佳位置：
- 不拦第一次“成功出图”（先让价值发生）
- 在“下载高清/批量/商用授权/云同步/高级风格库/深度模式无限换一批”处拦截

---

### 5. 指标体系（产品必须围绕指标迭代）

北极星指标：从首图成功到首次付费的转化率

关键漏斗：
- 首次进入 → 首次输入 → 首次成功出图 → 首次收藏/下载 → 触发付费墙 → 付费

体验指标（与成本挂钩）：
- 首图成功率、首图耗时 P50/P95、重试率、换一批次数、放弃率

---

### 6. 设计交付物清单（后续按此验收）

需要补齐的输出物：
- 色彩 Token（背景/文字/边框/状态/强调）
- 字体 Token（标题/正文/等宽/数字）
- 间距与栅格（8pt 系统 + 响应式断点）
- 组件规范（按钮/输入/卡片/弹窗/Toast/骨架屏）
- 动效规范（hover、面板展开、生成阶段、成功/失败反馈）





七、后端逻辑：积分系统的信任架构

积分系统听起来简单：用户充钱 → 加积分 → 生成图片 → 扣积分。

但魔鬼在细节里。

问题1：扣积分和生成图片，谁先谁后？

错误做法：先生成图片，成功后再扣积分。

风险：生成成功但扣积分失败，用户白嫖。
正确做法：先扣积分，失败后再退回。

逻辑：先"冻结"积分，生成成功则确认扣除，失败则退回。
// 伪代码
await deductCredits(userId, cost);  // 先扣
try {
  const image = await generateImage(prompt);
  return image;  // 成功，积分已扣
} catch (error) {
  await refundCredits(userId, cost);  // 失败，退回
  throw error;
}
问题2：如何防止订单重复处理？

爱发电的Webhook可能会重复发送。如果不做幂等处理，用户可能被多次加积分。

解决方案：用 afdianOrderId 做唯一键。

// 检查订单是否已处理
const existing = await prisma.transaction.findFirst({
  where: { afdianOrderId: orderId },
});
if (existing) {
  return { ec: 200, em: 'Already processed' };
}
问题3：免费额度怎么设计？

新用户应该能免费试用，否则付费转化无从谈起。

我的设计：注册即送 10 积分（够生成2次），每日签到送 2 积分（不够生成1次，但积累2天可以）。


这个数字是刻意的：

10 积分让用户体验核心功能
每日 2 积分制造"再来一次"的动机
但不够随便用，必须付费才能畅玩
积分系统的本质是信任系统：用户相信付了钱就能用，你相信用户不会薅走你的服务。