## 工程要求（面向盈利产品）

本文件用于统一“怎么做事、怎么验收、怎么上线”，目标是：尽快上线可收费版本，同时把质量、成本、合规做在系统里，而不是靠人肉记忆。

---

### 1) 项目边界与目标

目标：
- 做出可收费的 AI 产品（有明确权益、计费、对账、风控）
- 体验必须稳定、可控、可解释（尤其是失败与费用）
- 迭代必须可持续（模块化、可测试、可观测）

范围：
- 前端：`g:\AvuePro\newPro\frontend`（Vue 3 + TypeScript + Vite + Pinia + vue-router + ant-design-vue）
- 后端：`g:\AvuePro\newPro\backend\server.js`（Express）

---

### 2) 工作方式（保证速度与方向一致）

迭代节奏：
- 每次只做 2–5 个模块（可独立验收、可上线）
- 优先交付“主链路可收费”，其次才是扩展功能

定义完成（DoD）：
- 主链路可跑通（输入→出图/输出→收藏/下载→付费墙/额度提示）
- 失败有可操作提示（重试/降级/切模式）
- 成本可控（频控、冷却、上限、去重策略明确）
- 关键埋点齐全（转化漏斗与成本统计能对账）
- 无任何密钥泄漏（代码、日志、文档均禁止出现真实 key）

---

### 3) 代码组织（企业级拆分）

前端拆分原则：
- 组件（UI）/组合式逻辑（composables）/服务层（services）/类型（types）/数据（data）分离
- 一个功能域一个目录：例如 `src/agent`、后续 `src/agentImg`
- 服务端交互只在 services 层，组件不直接写请求细节

后端拆分原则（保持 server.js 可控）：
- 业务路由按领域拆分（后续可抽成 router 模块，但先不强制重构）
- 统一的错误结构与 requestId（便于排查与对账）
- 一切可计费动作都要写入用量台账（usage ledger）

---

### 4) 质量门槛（为了赚钱必须做的“硬约束”）

本地开发可以不强制跑完所有检查，但合并/上线必须满足：
- TypeScript 类型检查通过（前端 `vue-tsc`）
- 基础 lint 通过（至少避免明显错误与不一致）
- 核心流程有最少量回归验证（主链路 + 错误兜底）

原因：
- 盈利产品的真实成本不是“写代码”，而是“线上故障与客服成本”。

---

### 5) 安全与合规（必须写进流程）

密钥管理：
- 只允许通过环境变量配置（如 `SILICONFLOW_API_KEY`、`GEMINI_API_KEY`）
- 禁止把 key 写进任何文档、前端代码、日志输出
- 一旦泄漏：立刻作废并轮换密钥

用户数据：
- 明确保存范围（输入、Prompt、图片链接、用量台账）
- 明确删除/导出能力（至少 V1 要具备）

内容安全：
- 最低要求：敏感词与风险标签拦截
- 逐步升级：模型侧策略 + 人工申诉/黑白名单

---

### 6) 成本控制（盈利产品的生命线）

必须存在的规则：
- 深度思考/换一批/再来一张都有冷却与额度
- 默认参数偏省钱（步数、尺寸、张数），付费解锁上限
- 失败重试要做“降级重试”而不是无限重试

必须可观测：
- 每次请求的 tokens、耗时、模型、触发点、creditsDelta 可查询可对账
- 使用 `POST /api/usage/ingest` 作为统一入口

---

### 7) 发布与回滚（减少线上风险）

上线前：
- 先灰度（内部/小流量）验证核心漏斗
- 监控：错误率、耗时 P95、成本、付费转化

上线后：
- 任何影响成本的功能（换一批、批量出图、高清下载）必须可配置开关
- 出现异常可快速降级到“只输出方向/Prompt”
