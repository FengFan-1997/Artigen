## AgentImg（类 Agent 的文生图 Prompt 助手）产品与开发蓝图

### 1) 产品定位

一句话：让“懒得学提示词”的人，也能稳定出好图，并愿意为确定性付费。

目标人群：
- 想做头像/封面/配图/电商图/社媒图，但不懂专业提示词的人
- 有点审美但没有耐心调参的人
- 需要快速出图验证想法的小团队（自媒体、电商、设计助理）

核心卖点（对钱负责）：
- 低门槛：一句话输入 + 系统自动补全
- 高确定性：用“方向选择 + 可控参数 + 可复现 seed”减少随机性
- 可沉淀：收藏、复用、风格库、项目模板（形成资产）
- 可计费：每次关键动作都有成本与价值锚点（能量/积分体系）

参考 UI/UX 气质：https://www.nthme.org/（偏赛博/实验室/强叙事的视觉）

落地路径约束：
- 前端代码规划：放在 `g:\AvuePro\newPro\frontend\src\agentImg`（后续创建目录与路由时按企业级拆分）
- 技术栈：沿用现有前端（Vue 3 + TypeScript + Vite + Pinia + vue-router + ant-design-vue + 原生 CSS/LESS）与现有后端（Express）

---

### 2) 成功标准（盈利产品必须定义清楚）

北极星指标：
- “首图成功 → 触发付费墙 → 付费”的转化率

关键体验指标（决定留存/付费）：
- 首图成功率（目标：MVP ≥ 80%，V1 ≥ 90%）
- 首图耗时 P50 / P95（目标：P50 < 10s，P95 < 25s）
- 失败重试率（越低越好，但要区分“体验探索”与“被迫重试”）

单位经济模型（必须可算）：
- 单次深度思考（4 方向 + 换一批）消耗的模型成本
- 单次出图成本（按尺寸/步数/张数）
- 付费 ARPPU 与单用户平均消耗的比值（> 1 才能盈利）

---

### 3) 端到端流程（产品主路径）

主路径（默认模式）：
1. 用户输入一句话（模糊需求）
2. 系统套用本地 Prompt 模板库（结构化补全：主体/场景/镜头/光影/风格/材质/质量词/负面词）
3. 调用 LLM 把“用户话”翻译成“结构化意图 + Prompt 草案”
4. 直接生成 1 张预览图
5. 用户再来一张 / 调整参数 / 收藏 / 下载

深度思考模式（核心差异化）：
1. 用户输入一句话
2. LLM 生成 4 个“方向卡片”（不同风格/构图/情绪）
3. 用户选择 1 个方向
4. 系统基于该方向生成最终 Prompt（可展开查看）
5. 生成图片并展示
6. 支持“换一批”（重新生成 4 方向），但需要严格的频控与计费策略

必须有的兜底：
- 当模型不可用：仍然输出“方向卡片”或“Prompt”，不中断体验
- 当出图失败：提供“降级参数”（更小尺寸/更少步数）的一键重试

---

### 4) 功能模块拆解（MVP → V1 → V2）

#### MVP（先赚钱，再扩张，2–4 周）

用户可感知的功能：
- 输入 + 方向卡片（深度模式可开关）+ 出图 + 结果画廊
- 收藏/历史（本地即可，先形成“资产感”）
- 下载（默认带水印；付费解锁无水印/高清）
- 计费与用量展示（能量/积分，先做可计费再做花样）

系统能力（必须做，不然赚不到钱）：
- Prompt 模板库（本地 JSON/TS 模块化管理）
- 成本控制：换一批、再来一张、并发、超时、降级
- 失败可解释：错误提示可操作（重试/降级/切换模式）

#### V1（付费产品形态，4–8 周）

提升付费意愿的功能：
- 个人风格偏好（保存：风格、画幅、用途、常用负面词）
- 项目模板（社媒封面/电商主图/头像/插画等）
- 收藏云同步（与账号绑定）
- 版本对比（同一 Prompt 的多次出图对比）

运营必需：
- 新手引导（3 步）+ 示例库 + 空状态设计
- 付费墙（节奏正确：先价值后收费）
- 基础数据面板（转化漏斗 + 失败率 + 成本）

#### V2（规模化与护城河，8–12 周）

护城河功能：
- 用户自建风格库（上传参考图/描述 → 生成可复用模板）
- 团队空间（多人共享模板与素材）
- 商用授权与版权说明（可定价的关键）

---

### 5) 技术与接口规划（对齐现有后端实现）

现有后端能力（Express）已经具备：
- 通用文本生成：`POST /api/generate`
- Agent 对话与上下文：`POST /api/chat`
- 账号注册登录：`POST /api/auth/register` / `POST /api/auth/login`
- 用量台账：`POST /api/usage/ingest`、`GET /api/usage/ledger`、`GET /api/usage/summary`
- 频控：服务端 rate limit

AgentImg 需要新增的后端接口（后续开发时做）：
- `POST /api/agentimg/options`：输入 + deepMode → 返回 4 个方向卡片（含标题、关键词、风格、建议参数）
- `POST /api/agentimg/prompt`：锁定方向 + 用户补充 → 返回最终 Prompt（含负面词、结构化参数）
- `POST /api/agentimg/image`：最终 Prompt + 参数 → 调用生图服务并返回图片 URL/Base64
- `GET /api/agentimg/history`：按 userId 获取历史（MVP 可先用本地存储，V1 再上云）

密钥与环境变量（安全要求）：
- 只允许通过环境变量配置：`SILICONFLOW_API_KEY`、`GEMINI_API_KEY` 等
- 文档与前端代码中禁止出现任何真实密钥

---

### 6) 数据结构（建议）

PromptTemplate（本地库）：
- id、用途（avatar/banner/product）、风格标签、模板片段（正向/负向）、默认参数

DirectionOption（方向卡片）：
- id、title、summary、styleTags、promptDraft、suggestedParams、riskFlags（NSFW/敏感）

GenerationJob（一次生成）：
- requestId、userId、sessionId、mode（normal/deep）、input、finalPrompt、params、result、status、durationMs、creditsDelta

---

### 7) 计费与规则（必须写清楚，才能上线收费）

计费单位：
- Energy/Credits：所有消耗统一映射到 credits（便于套餐设计）

建议的消耗点（示例，不是最终数值）：
- 深度思考：每次生成 4 方向 = X credits
- 换一批：每次 = X credits（且有冷却/每日上限）
- 出图：按尺寸/步数/张数阶梯计费

防“成本爆炸”的硬规则：
- 新用户每日免费额度（用来体验价值，但严格封顶）
- 同一输入短时间重复触发，做去重或提示复用
- 默认参数偏省钱（步数、尺寸），付费解锁更高质量上限

---

### 8) 里程碑与排期（建议版本节奏）

第 1 周：MVP 主流程跑通（输入→Prompt→出图→展示→失败兜底）
第 2 周：深度模式（4 方向 + 换一批 + 选择锁定）+ 收藏/历史（本地）
第 3 周：付费墙与积分（能量体系、套餐页、权益展示、用量统计接入）
第 4 周：体验打磨（引导、空状态、加载、错误、性能）+ 上线试运营

---

### 9) 风险清单（提前写进文档，减少翻车）

- Prompt 注入与越权：任何“系统提示词/密钥/内部逻辑”不得被模型输出
- NSFW/敏感内容：需要检测与拒绝策略（至少先做关键词与模型侧规则）
- 法务与版权：商用授权、素材来源、用户生成内容归属，必须有明确说明
- 成本与滥用：频控、冷却、上限、去重、账单可追溯

